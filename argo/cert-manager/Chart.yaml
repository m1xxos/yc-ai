apiVersion: v2
name: cert-manager
description: cert-manager with Let's Encrypt HTTP-01 challenge
version: 1.0.0
type: application

dependencies:
- name: cert-manager
  version: "v1.20.0"
  repository: "https://charts.jetstack.io"
